#2 - Allio V1 Code Sync & Secrets Migration
What & Why
Sync all source code from the Allio v1 GitHub repo (bfiveash22/ffpma-alliov1, commit f51e18476a4cb6f0d169546dc5b9792d7a82e2f7) into the Replit project's ffpma-app/ directory, and migrate all secrets from the VPS .env into Replit's secret manager. The GitHub/VPS version is the source of truth for v1 code. The Replit project has additional new work (protocol assembly, intake forms, blood analysis, Rupa Health, calendar, Slides, etc.) that must NOT be overwritten.

Done looks like
All v1 files from GitHub are present in ffpma-app/ — specifically the missing server/replit_integrations/ directory (14 files) and server/routes-full.ts
Files that exist in both GitHub and Replit are compared; any that differ get the VPS version merged in without losing new Replit-only additions
All secrets from the VPS .env (attached_assets/_1773395331589.env) are added to Replit Secrets — including AbacusAI, Rupa Health, SignNow extras, Google OAuth, ENABLE_SCHEDULERS
The Google Refresh Token uses the VPS value (most current): 1//0fteL4Gvo-McQ...
Plaintext .env, env.yaml, and env-template-2026-03-09.txt files are removed from ffpma-app/ after secrets are in Replit
The client_secret_*.json Google OAuth file is removed (credentials moved to secrets)
Code that reads from .env files directly (dotenv) is updated to read from process.env (Replit injects secrets automatically)
All existing new work in Replit (protocol assembly, intake, blood analysis, Rupa Health agent, calendar, slides, Google ML, etc.) remains intact and functional
Out of scope
Restructuring the monorepo layout (ffpma-app stays as-is for now)
Migrating the app to the artifacts/ directory structure
Changing how services connect to external APIs (just ensuring secrets are available)
Updating the deployed/production configuration
Tasks
Pull the 15 missing files from GitHub into ffpma-app/ — the server/replit_integrations/ directory tree (auth, batch, chat, image) and server/routes-full.ts
Compare all shared files (routes.ts, storage.ts, storage-full.ts, schema.ts, server/index.ts, all service files, all client pages) between GitHub and Replit — for files that differ, merge the VPS content while preserving any Replit-only additions
Add all secrets from the VPS .env to Replit Secrets — the full list: OPENAI_API_KEY, GEMINI_API_KEY, GOOGLE_GEMINI_API_KEY, ABACUSAI_API_KEY, DATABASE_URL, SESSION_SECRET, WP_SITE_URL, WP_USERNAME, WP_APPLICATION_PASSWORD, HUGGINGFACE_API_KEY, PUBMED_API_KEY, CORE_API_KEY, WC_CONSUMER_KEY, WC_CONSUMER_SECRET, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REFRESH_TOKEN (use VPS value), SIGNNOW_CLIENT_ID, SIGNNOW_BAT_TOKEN, SIGNNOW_APP_ID, SIGNNOW_SECRET_KEY, SIGNNOW_EMAIL, SIGNNOW_PASSWORD, ENABLE_SCHEDULERS, RUPA_USERNAME, RUPA_PASSWORD
Remove plaintext secret files from ffpma-app/: .env, env.yaml, env-template-2026-03-09.txt, and the client_secret_*.json file
Update any dotenv loading code (e.g., import dotenv / dotenv.config()) to work without a .env file — Replit injects secrets into process.env automatically
Verify the app starts without errors and all services can read their required environment variables
Relevant files
attached_assets/_1773395331589.env
ffpma-app/.env
ffpma-app/env.yaml
ffpma-app/env-template-2026-03-09.txt
ffpma-app/server/index.ts
ffpma-app/server/routes.ts
ffpma-app/server/storage.ts
ffpma-app/server/storage-full.ts
ffpma-app/server/db.ts
ffpma-app/server/services/signnow.ts
ffpma-app/server/services/woocommerce.ts
ffpma-app/server/services/wordpress-auth.ts
ffpma-app/server/services/drive.ts
ffpma-app/server/services/gmail.ts
ffpma-app/shared/schema.ts
ffpma-app/client/src/App.tsx